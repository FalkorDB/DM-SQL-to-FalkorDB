apiVersion: v1
kind: Service
metadata:
  name: {{ include "dm-sql-to-falkordb.fullname" . }}
  labels:
    {{- include "dm-sql-to-falkordb.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dm-sql-to-falkordb.selectorLabels" . | nindent 4 }}
